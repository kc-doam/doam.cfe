
#Область ПрограммныйИнтерфейс

// Метод добавлен для обеспечения работы подсистем ЭП версии БСП 3.1.10.295
// Записывает в журнал регистрации событие при выполнении обработчика обновления.
// При записи ошибки или предупреждения сохраняет эту информацию в сведениях об обработчике
// обновления для дальнейшей индикации в интерфейсах механизма обновления.
//
// Параметры:
//  СсылкаМетаданные - ЛюбаяСсылка, ОбъектМетаданных - для ссылочных объектов ссылка, для остальных метаданные
//  Представление    - Строка - строковое представление обрабатываемых данных. Для ссылок необходимо получать его до
//                                 начала обработки, т.к. получение в блоке исключения может приводить к ошибке.
//  ИнформацияОбОшибке - ИнформацияОбОшибке, Строка - можно передавать объект ИнформацияОбОшибке для записи
//                                 стандартной ошибки в журнал регистрации. Для уточнения текста проблемы
//                                 стоит передавать строковый комментарий.
//  Уровень - УровеньЖурналаРегистрации - уровень важности события. Если не указано, то записывается событие с уровнем Предупреждение.
//
Процедура ЗаписатьОшибкуВЖурналРегистрации(СсылкаМетаданные, Знач Представление, ИнформацияОбОшибке = Неопределено, Уровень = Неопределено) Экспорт
	
	Данные = Неопределено;
	ОбъектМетаданных = Неопределено;
	Если ОбщегоНазначения.ЭтоСсылка(ТипЗнч(СсылкаМетаданные)) Тогда
		Данные = СсылкаМетаданные;
		ОбъектМетаданных = СсылкаМетаданные.Метаданные();
	Иначе
		ОбъектМетаданных = СсылкаМетаданные;
	КонецЕсли;
	
	ТекстОшибки = "";
	Если ТипЗнч(ИнформацияОбОшибке) = Тип("Строка") Тогда
		ТекстОшибки = ИнформацияОбОшибке;
	Иначе
		ТекстОшибки = ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке);
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(Представление) Тогда
		Представление = ОбъектМетаданных.Имя;
	КонецЕсли;
	
	ТекстСообщения = НСтр("ru = 'Не удалось обработать ""%1"" по причине:
		|%2'");
	ТекстСообщения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(ТекстСообщения,
		Представление,
		ТекстОшибки);
	
	ЗаписьЖурналаРегистрации(СобытиеЖурналаРегистрации(),
		УровеньЖурналаРегистрации.Предупреждение,
		ОбъектМетаданных,
		Данные,
		ТекстСообщения);
	
	ИмяОбработчика = ПараметрыСеанса.ПараметрыОбработчикаОбновления.ИмяОбработчика;
	Если Уровень = УровеньЖурналаРегистрации.Ошибка
		Или Уровень = Неопределено Тогда
		ОбновлениеИнформационнойБазыСлужебный.ДобавитьИнформациюОбОшибкеВОбработчике(ИмяОбработчика);
	ИначеЕсли Уровень = УровеньЖурналаРегистрации.Предупреждение Тогда
		ОбновлениеИнформационнойБазыСлужебный.ДобавитьИнформациюОбОшибкеВОбработчике(ИмяОбработчика);
	КонецЕсли;
	
КонецПроцедуры

// Метод добавлен для обеспечения работы подсистем ЭП версии БСП 3.1.10.295
// Возвращает строковую константу для формирования сообщений журнала регистрации.
//
// Возвращаемое значение:
//   Строка - текст события журнала регистрации.
//
Функция СобытиеЖурналаРегистрации() Экспорт
	
	Возврат ОбновлениеИнформационнойБазыСлужебный.СобытиеЖурналаРегистрации();
	
КонецФункции

// Метод добавлен для обеспечения работы БЭД 1.9.15
// Получает подписи объекта и возвращает их.
//
// Параметры:
//  Объект - ОпределяемыйТип.ПодписанныйОбъект - ссылка на подписанный объект.
//             Объект должен иметь реквизит ПодписанЭП.
//
//  ДополнительныеПараметры - см. НовыйПараметрыПолученияПодписейОбъекта
//
// Возвращаемое значение:
//  Массив из см. ЭлектроннаяПодписьКлиентСервер.НовыеСвойстваПодписи 
//
Функция ПодписиОбъекта(Объект, ДополнительныеПараметры = Неопределено) Экспорт
	
	Если ДополнительныеПараметры = Неопределено Тогда
		ДополнительныеПараметры = НовыйПараметрыПолученияПодписейОбъекта();
	КонецЕсли;
	
	ПроверитьПараметрОбъект(Объект, "ЭлектроннаяПодпись.ПодписиОбъекта", Истина);
	Если ОбщегоНазначения.ПодсистемаСуществует("СтандартныеПодсистемы.УправлениеДоступом") Тогда
		МодульУправлениеДоступом = ОбщегоНазначения.ОбщийМодуль("УправлениеДоступом");
		МодульУправлениеДоступом.ПроверитьЧтениеРазрешено(Объект);
	КонецЕсли;
	
	Если ОбщегоНазначения.ЭтоСсылка(ТипЗнч(Объект)) Тогда
		ОбъектСсылка = Объект;
	Иначе
		ОбъектСсылка = Объект.Ссылка;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Истина);
	
	РаботаСЭП.ПередЧтениемЭПОбъекта(ОбъектСсылка);
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	ЭлектронныеПодписи.Подпись КАК Подпись,
		|	ЭлектронныеПодписи.ПорядковыйНомер КАК ПорядковыйНомер,
		|	ЭлектронныеПодписи.УстановившийПодпись КАК УстановившийПодпись,
		|	ЭлектронныеПодписи.Комментарий КАК Комментарий,
		|	ЭлектронныеПодписи.ДатаПодписи КАК ДатаПодписи,
		|	ЭлектронныеПодписи.ДатаПроверкиПодписи КАК ДатаПроверкиПодписи,
		|	ЭлектронныеПодписи.ПодписьВерна КАК ПодписьВерна,
		|	ЭлектронныеПодписи.Отпечаток КАК Отпечаток,
		|	ЭлектронныеПодписи.КомуВыданСертификат КАК КомуВыданСертификат,
		|	ЭлектронныеПодписи.ТипПодписи КАК ТипПодписи,
		|	ЭлектронныеПодписи.ТребуетсяПроверка КАК ТребуетсяПроверка,
		|	ЭлектронныеПодписи.СрокДействияПоследнейМеткиВремени КАК СрокДействияПоследнейМеткиВремени,
		|	ЭлектронныеПодписи.ИдентификаторПодписи КАК ИдентификаторПодписи,
		|	ЭлектронныеПодписи.ПодписьМатематическиВерна,
		|	ЭлектронныеПодписи.ОшибкаМатематическойПроверкиПодписи,
		|	ЭлектронныеПодписи.ОшибкаПроверкиДополнительныхАтрибутов,
		|	ЭлектронныеПодписи.ДополнительныеАтрибутыПровереныВручную,
		|	ЭлектронныеПодписи.АвторРучнойПроверкиДополнительныхАтрибутов,
		|	ЭлектронныеПодписи.ОбоснованиеРучнойПроверкиДополнительныхАтрибутов
		|ИЗ
		|	РегистрСведений.ЭлектронныеПодписи КАК ЭлектронныеПодписи
		|ГДЕ
		|	ЭлектронныеПодписи.ПодписанныйОбъект = &ПодписанныйОбъект
		|	И ЭлектронныеПодписи.ПорядковыйНомер В (&ПорядковыйНомер)
		|
		|УПОРЯДОЧИТЬ ПО
		|	ПорядковыйНомер";
	
	Запрос.УстановитьПараметр("ПодписанныйОбъект", ОбъектСсылка);
	
	Если ДополнительныеПараметры.ПорядковыйНомер = Неопределено Тогда
		Запрос.Текст = СтрЗаменить(Запрос.Текст, "И ЭлектронныеПодписи.ПорядковыйНомер В (&ПорядковыйНомер)", "");
	Иначе
		Запрос.УстановитьПараметр("ПорядковыйНомер", ДополнительныеПараметры.ПорядковыйНомер);
	КонецЕсли;
	
	РезультатЗапроса = Запрос.Выполнить();
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	// СтандартныеПодсистемы.МашиночитаемыеДоверенности
	ВыборкаДетальныеЗаписиМЧД = Неопределено;
	
	Если Не РезультатЗапроса.Пустой() И ДополнительныеПараметры.ВозвращатьДанныеМЧД
		И ОбщегоНазначения.ПодсистемаСуществует("СтандартныеПодсистемы.МашиночитаемыеДоверенности") Тогда
		
		ИдентификаторыПодписей = Новый Массив;
		Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
			ИдентификаторыПодписей.Добавить(ВыборкаДетальныеЗаписи.ИдентификаторПодписи);
		КонецЦикла;
		ВыборкаДетальныеЗаписи.Сбросить();
		
		МодульМашиночитаемыеДоверенностиФНССлужебный = ОбщегоНазначения.ОбщийМодуль("МашиночитаемыеДоверенностиФНССлужебный");
		ВыборкаДетальныеЗаписиМЧД = МодульМашиночитаемыеДоверенностиФНССлужебный.МЧДПодписей(ОбъектСсылка, ИдентификаторыПодписей);
		
	КонецЕсли;
	// Конец СтандартныеПодсистемы.МашиночитаемыеДоверенности
	
	РасширениеДляФайловПодписи = ЭлектроннаяПодпись.ПерсональныеНастройки().РасширениеДляФайловПодписи;
	Результат = Новый Массив;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		
		СвойстваПодписи = ЭлектроннаяПодписьКлиентСервер.НовыеСвойстваПодписи();
		СвойстваПодписи.ПодписанныйОбъект = Объект;
		ЗаполнитьЗначенияСвойств(СвойстваПодписи, ВыборкаДетальныеЗаписи);
		СвойстваПодписи.Подпись = СвойстваПодписи.Подпись.Получить();
		
		ИмяПодписанногоОбъекта = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОбъектСсылка, "Наименование");
		СвойстваПодписи.ИмяФайлаПодписи = ИмяФайлаПодписи(ИмяПодписанногоОбъекта,
			ВыборкаДетальныеЗаписи.КомуВыданСертификат, РасширениеДляФайловПодписи);
		
		Если ДополнительныеПараметры.ВозвращатьСертификатыИзПодписей Тогда
			СвойстваПодписи.Сертификат = СертификатИзДвоичныхДанныхПодписи(СвойстваПодписи.Подпись);
		КонецЕсли;
		
		// СтандартныеПодсистемы.МашиночитаемыеДоверенности
		Если ВыборкаДетальныеЗаписиМЧД <> Неопределено Тогда
			МодульМашиночитаемыеДоверенностиФНССлужебный.ЗаполнитьДанныеМЧДПодписи(СвойстваПодписи, ВыборкаДетальныеЗаписиМЧД);
		КонецЕсли;
		// Конец СтандартныеПодсистемы.МашиночитаемыеДоверенности
		
		Результат.Добавить(СвойстваПодписи);
		
	КонецЦикла;
	
	Возврат Результат;
	
КонецФункции

// Метод добавлен для обеспечения работы БЭД 1.9.15
// Конструктор для описания дополнительных параметров получения электронных подписей объекта.
// 
// Возвращаемое значение:
//   Структура:
//     * ПорядковыйНомер               - Число - порядковый номер подписи для отбора.
//                                     - Массив из Число - массив из порядковых номеров подписи для отбора.
//     * ВозвращатьДанныеМЧД           - Булево - если Истина и для подписи есть МЧД, будет заполнено свойство РезультатПроверкиПодписиПоМЧД
//     * ВозвращатьСертификатыИзПодписей - Булево - если Истина, будет выполнено получение сертификатов из двоичных
//                                                  данных подписей.
//
Функция НовыйПараметрыПолученияПодписейОбъекта() Экспорт
	
	Структура = Новый Структура;
	Структура.Вставить("ПорядковыйНомер", Неопределено);
	Структура.Вставить("ВозвращатьДанныеМЧД", Ложь);
	Структура.Вставить("ВозвращатьСертификатыИзПодписей", Ложь);
	
	Возврат Структура;
	
КонецФункции

// Метод добавлен для обеспечения работы БЭД 1.9.15
// Извлекает сертификат из двоичных данных подписи.
// 
// Параметры:
//  Подпись						 - ДвоичныеДанные - результат подписания
//  РезультатВХранилищеЗначений	 - Булево - помещать результат в ХранилищеЗначения
// 
// Возвращаемое значение:
//   ДвоичныеДанные - двоичные данные сертификата.
//   Неопределено - подпись не содержит сертификатов криптографии.
//   ХранилищеЗначения - результат дополнительно помещен в ХранилищеЗначения.
//
Функция СертификатИзДвоичныхДанныхПодписи(Подпись, РезультатВХранилищеЗначения = Ложь) Экспорт
	
	СвойстваПодписи = ЭлектроннаяПодписьСлужебный.СвойстваПодписиИзДвоичныхДанных(Подпись, Истина);
	
	Возврат ?(РезультатВХранилищеЗначения,
		Новый ХранилищеЗначения(СвойстваПодписи.Сертификат, Новый СжатиеДанных(9)),
		СвойстваПодписи.Сертификат);
	
КонецФункции

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Код процедур и функций

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Дубль неэкспортного метода БСП.
Процедура ПроверитьПараметрОбъект(Объект, ИмяПроцедуры, ТолькоСсылки = Ложь)
	
	ОбщегоНазначенияКлиентСервер.ПроверитьПараметр(ИмяПроцедуры, "Объект", Объект,
		ЭлектроннаяПодписьСлужебныйПовтИсп.ТипыВладельцев(ТолькоСсылки));
	
КонецПроцедуры

// Метод добавлен для обеспечения работы БЭД 1.9.15
// Формирует имя файла подписи по шаблону.
//
Функция ИмяФайлаПодписи(ИмяБезРасширения, КомуВыданСертификат, РасширениеДляФайловПодписи, ТребуетсяРазделитель = Истина) Экспорт
	
	Разделитель = ?(ТребуетсяРазделитель, " - ", " ");
	
	ИмяФайлаПодписиБезРасширения = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку("%1%2%3",
		ИмяБезРасширения, Разделитель, КомуВыданСертификат);
	
	Если СтрДлина(ИмяФайлаПодписиБезРасширения) > 120 Тогда
		ИмяФайлаПодписиБезРасширения = ЭлектроннаяПодписьСлужебныйВызовСервера.СокращенноеИмяФайла(
			ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыВИмениФайла(ИмяФайлаПодписиБезРасширения), 120);
	КонецЕсли;
	
	ИмяФайлаПодписи = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку("%1.%2",
		ИмяФайлаПодписиБезРасширения, РасширениеДляФайловПодписи);
	
	Возврат ОбщегоНазначенияКлиентСервер.ЗаменитьНедопустимыеСимволыВИмениФайла(ИмяФайлаПодписи);

КонецФункции

#КонецОбласти
