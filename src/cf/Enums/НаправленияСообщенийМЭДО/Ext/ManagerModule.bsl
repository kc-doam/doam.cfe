#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область СлужебныйПрограммныйИнтерфейс

// Функция - Женский род от названия перечисления
//
// Параметры:
//  Направление	 - ПеречислениеСсылка.НаправленияСообщенийМЭДО	- Направление, которое надо преобразовать
// 
// Возвращаемое значение:
//  Строка
//
Функция ЖенскийРод(Направление) Экспорт
	
	Если Направление = Входящее Тогда
		Возврат НСтр("ru = 'Входящая'");
	ИначеЕсли Направление = Исходящее Тогда
		Возврат НСтр("ru = 'Исходящая'");
	КонецЕсли;
	
	Возврат "";
	
КонецФункции

// Направление документа.
// 
// Параметры:
//  Документ - ОпределяемыйТип.ПредметМЭДО - Документ
// 
// Возвращаемое значение:
//  ПеречислениеСсылка.НаправленияСообщенийМЭДО - Направление документа
Функция НаправлениеДокумента(Документ) Экспорт
	
	Если Не ЗначениеЗаполнено(Документ) Тогда
		Возврат Входящее; // Для исходящих уведомлений об отказе - входящий документ может быть не заполнен
						  // (не загрузился из-за ошибки).
	КонецЕсли;
	
	Если МЭДОПереопределяемый.ЭтоВходящийДокумент(Документ) Тогда
		Возврат Входящее;
	ИначеЕсли МЭДОПереопределяемый.ЭтоИсходящийДокумент(Документ) Тогда
		Возврат Исходящее;
	Иначе
		Возврат ПустаяСсылка();
	КонецЕсли;
	
КонецФункции

#КонецОбласти

#КонецЕсли
