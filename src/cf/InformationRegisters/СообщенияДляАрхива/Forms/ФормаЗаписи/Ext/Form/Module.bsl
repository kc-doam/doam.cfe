
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Запрос = Новый Запрос(
		"ВЫБРАТЬ
		|	СообщенияДляАрхива.ТелоСообщения КАК ТелоСообщения
		|ИЗ
		|	РегистрСведений.СообщенияДляАрхива КАК СообщенияДляАрхива
		|ГДЕ
		|	СообщенияДляАрхива.ИдентификаторСообщения = &ИдентификаторСообщения");
	Запрос.УстановитьПараметр("ИдентификаторСообщения", Запись.ИдентификаторСообщения);
	Выборка = Запрос.Выполнить().Выбрать();
	Если Выборка.Следующий() Тогда
		ТелоСтрокой = Выборка.ТелоСообщения.Получить();
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
