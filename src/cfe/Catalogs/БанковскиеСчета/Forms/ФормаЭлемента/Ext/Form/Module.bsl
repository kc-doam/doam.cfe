
&НаКлиенте
Процедура РасшДОАМ_ПередЗаписьюПосле(Отказ, ПараметрыЗаписи)
	
	Если  Элементы.Наименование.СписокВыбора.Количество() > 1 Тогда
		Объект.Наименование = Элементы.Наименование.СписокВыбора[0];
	Иначе
		Объект.Наименование = "Банковский счет";
	КонецЕсли;
	
КонецПроцедуры
