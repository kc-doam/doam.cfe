
&НаСервере
Процедура РасшДОАМ_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	НовыйЭлемент 							= Элементы.Вставить("ВыводитьНаНачальной", Тип("ПолеФормы"), Элементы.НачальнаяСтраница);
	НовыйЭлемент.Вид						= ВидПоляФормы.ПолеФлажка;
	НовыйЭлемент.ПутьКДанным				= "Объект.ВыводитьНаНачальной";
	НовыйЭлемент.Заголовок					= "Выводить на начальной";

	НовыйЭлемент 							= Элементы.Вставить("ВыполнятьПоиск", Тип("ПолеФормы"), Элементы.НачальнаяСтраница);
	НовыйЭлемент.Вид						= ВидПоляФормы.ПолеФлажка;
	НовыйЭлемент.ПутьКДанным				= "Объект.ВыполнятьПоиск";
	НовыйЭлемент.Заголовок					= "Выполнять поиск";

	НовыйЭлемент 							= Элементы.Вставить("ВыборСпискаЗначений", Тип("ДекорацияФормы"), Элементы.НачальнаяСтраница);
	НовыйЭлемент.Вид						= ВидДекорацииФормы.Надпись;
	НовыйЭлемент.Заголовок					= ?(ЗначениеЗаполнено(Объект.НаборСвойств), "", "Возможность выбирать несколько значений");
	
КонецПроцедуры
